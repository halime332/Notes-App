import type { FC } from "react"
import Button from '@mui/material/Button';
import { styled } from "@mui/material";

const Main:FC = () => {
  return (
    <div>
     <h1>Main</h1>
    </div>
  )
};

export default Main;
